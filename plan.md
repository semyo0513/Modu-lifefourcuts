# 인생네컷 웹앱 제작 계획서 (plan.md)

> 프레임 선택 → 6컷 촬영 → 보정/선택/편집 → 인쇄·이메일 전송까지 이어지는 브라우저 기반 포토부스 웹앱.
> GitHub 저장소에 정적 파일로 저장하고 GitHub Pages로 배포하는 것을 전제로 설계함.

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 목적 | 실제 인생네컷 매장과 유사한 촬영~인화 경험을 웹앱으로 구현 |
| 배포 방식 | GitHub Pages (정적 호스팅, 백엔드 서버 없음) |
| 대상 기기 | 노트북(웹캠 연결) / 모바일(전면 카메라) 모두 지원 |
| 핵심 제약 | 서버가 없으므로 **인쇄·이메일 전송을 모두 클라이언트(브라우저) 기술만으로 해결**해야 함 |

---

## 2. 사용자 흐름 (User Flow)

```
[시작 화면]
    ↓
[1단계] 프레임 선택 (PNG 프레임 갤러리에서 1개 선택)
    ↓
[2단계] 카메라 권한 요청 → 6컷 순차 촬영 (카운트다운 포함)
    ↓
[3단계] 보정 (밝기/대비/채도/필터 프리셋) → 6장 중 프레임 슬롯 수만큼 선택 → 순서(위치) 배정
    ↓
[4단계] 프레임에 합성된 최종 결과 미리보기
    ↓
[5단계] [인쇄하기] 또는 [이메일로 전송하기] → 완료
```

---

## 3. 기능 분석 (상세 명세)

### 3.1 프레임 선택

- `frames/` 폴더에 PNG 프레임 이미지들을 미리 업로드해두고, `frames.json` 매니페스트로 목록과 메타데이터를 관리
- 각 프레임 PNG는 **사진이 들어갈 자리가 투명(alpha)으로 뚫려 있는 오버레이 이미지**여야 함 (프레임을 사진 "위에" 얹어 합성)
- `frames.json`에는 프레임별로 다음 정보를 정의:
  - 캔버스 전체 크기(px)
  - 사진 슬롯 개수와 각 슬롯의 좌표(x, y, width, height)
  - 슬롯별 사진 회전 여부(선택)

```json
[
  {
    "id": "frame_pink",
    "name": "핑크 리본",
    "file": "frames/frame_pink.png",
    "canvas": { "width": 600, "height": 1800 },
    "slots": [
      { "x": 40, "y": 40,  "w": 520, "h": 380 },
      { "x": 40, "y": 460, "w": 520, "h": 380 },
      { "x": 40, "y": 880, "w": 520, "h": 380 },
      { "x": 40, "y": 1300,"w": 520, "h": 380 }
    ]
  }
]
```

> 슬롯 개수는 프레임마다 다를 수 있음(2컷/4컷 등). 사용자는 6컷을 찍은 뒤, **선택한 프레임의 슬롯 수만큼만** 사진을 골라 배치.

### 3.2 촬영 (6컷) · 보정 · 선택 · 순서 설정

| 기능 | 구현 방식 |
|---|---|
| 카메라 프리뷰 | `navigator.mediaDevices.getUserMedia()`로 `<video>` 스트림 표시 |
| 6컷 순차 촬영 | 3-2-1 카운트다운 → `<canvas>`에 프레임 캡처 → 자동으로 다음 컷 진행, 총 6회 반복 |
| 기본 보정 | 슬라이더(밝기/대비/채도)와 프리셋 필터(예: 필름톤, 흑백, 화사하게)를 CSS `filter` 또는 Canvas 픽셀 연산으로 적용 |
| 사진 선택 | 촬영된 6장 썸네일을 그리드로 표시, 탭/클릭으로 필요한 장수만큼 체크 |
| 순서 설정 | 선택된 사진을 드래그 앤 드롭(또는 화살표 버튼)으로 프레임 슬롯 순서에 맞게 재배열 |

- 보정은 실제 인생네컷 매장 수준의 "가벼운 뽀샤시" 정도를 목표로 하며, 정밀 리터칭(피부 잡티 제거 등)은 범위에서 제외 — 과도한 기대를 방지하기 위해 처음부터 범위를 명확히 함

### 3.3 완성 · 인쇄 · 이메일 전송

- 선택된 사진들을 프레임의 슬롯 좌표에 맞춰 `<canvas>`에 합성 → 프레임 PNG를 최상단에 오버레이
- 완성 이미지를 `canvas.toDataURL()` / `toBlob()`으로 추출해 미리보기 제공
- **[인쇄하기]**, **[이메일로 전송하기]** 두 버튼 제공 (구현 전략은 4, 5장에서 별도 설명)

---

## 4. 인쇄 기능 구현 전략 (핵심 — 기술적 한계와 해결책)

먼저 정확히 짚어야 할 제약이 있습니다: **브라우저 JavaScript는 보안상 어떤 프린터가 연결되어 있는지 직접 조회하거나, 사용자 확인 없이 바로 인쇄를 실행할 수 없습니다.** 이는 특정 라이브러리의 한계가 아니라 모든 브라우저의 공통 정책입니다. 따라서 "바로 연결되어 인쇄"는 **OS/브라우저가 이미 알고 있는 프린터 목록을 자동으로 띄워주는 방식**으로 구현하는 것이 현실적이고 가장 안정적인 해법입니다. 기기별로 다음과 같이 분기합니다.

| 환경 | 방식 | 사용자 경험 |
|---|---|---|
| 노트북(데스크톱) | `window.print()` + `@media print` CSS | 브라우저 인쇄 대화상자가 열리고, OS에 이미 연결/등록된 프린터(USB, 와이파이, 네트워크 프린터)가 자동으로 목록에 표시됨. 용지 크기를 인생네컷 스트립 규격(예: 2×6인치)에 맞춰 CSS `@page`로 지정 |
| 모바일 | Web Share API (`navigator.share`, 파일 공유) 우선 시도 → 실패 시 `window.print()` 폴백 | 완성 이미지를 OS 공유 시트로 전달하면, iOS는 AirPrint, Android는 시스템에 등록된 프린터 서비스(제조사 프린트 서비스 등)가 공유 메뉴의 "인쇄" 항목에 자동으로 뜸. 이 방식이 모바일 브라우저의 `window.print()`보다 실제 프린터 연결 성공률이 높음 |

```javascript
async function handlePrint(imageBlob) {
  const file = new File([imageBlob], "life4cut.png", { type: "image/png" });
  if (navigator.canShare && navigator.canShare({ files: [file] })) {
    // 모바일: OS 공유 시트 → AirPrint/등록된 프린터로 인쇄
    await navigator.share({ files: [file], title: "인생네컷" });
  } else {
    // 데스크톱: 인쇄 전용 뷰를 열고 브라우저 인쇄 대화상자 실행
    openPrintView(imageBlob);
    window.print();
  }
}
```

```css
/* 인쇄 시 스트립 실제 규격(2x6인치)에 맞춰 여백 없이 출력 */
@media print {
  @page { size: 2in 6in; margin: 0; }
  body * { visibility: hidden; }
  #print-strip, #print-strip * { visibility: visible; }
  #print-strip { position: fixed; top: 0; left: 0; width: 2in; height: 6in; }
}
```

**참고(확장 옵션, 이번 범위 밖)**: 특정 모델의 블루투스 감열식 포토프린터(예: 미니 포토프린터)를 전용으로 연동하려면 Web Bluetooth/Web USB API로 해당 프린터의 자체 프로토콜(ESC/POS 등)을 직접 구현해야 하며, 이는 프린터 모델마다 SDK가 달라 별도 프로젝트 수준의 작업입니다. 특정 프린터 기종이 정해지면 이 부분은 Phase 5 이후 별도로 검토하는 것을 권장합니다.

---

## 5. 이메일 전송 기능 구현 전략

GitHub Pages는 백엔드가 없는 정적 호스팅이므로, 서버 없이 클라이언트에서 이메일을 보낼 수 있는 **EmailJS**를 사용하는 것을 권장합니다.

- 완성된 이미지를 base64로 인코딩해 이메일 템플릿에 첨부파일로 전달 → EmailJS가 연결된 Gmail 등 계정을 통해 실제 발송
- 무료 플랜은 월 200건 전송까지 가능하며, 발신 이메일 하단에 EmailJS 안내 푸터가 붙습니다. 첨부파일 용량은 낮은 등급 플랜일수록 제한이 있으므로, 전송 전 `canvas.toBlob(..., "image/jpeg", 0.8)` 등으로 이미지를 적절히 압축하는 처리를 함께 넣는 것을 권장합니다.
- API 키(Public Key)는 클라이언트에 노출되는 구조이므로, EmailJS 콘솔에서 **도메인 화이트리스트**(내 GitHub Pages 주소만 허용)를 반드시 설정해 오남용을 방지합니다.

```javascript
async function handleEmailSend(imageBlob, toEmail) {
  const base64 = await blobToBase64(imageBlob); // "data:image/jpeg;base64,...."
  await emailjs.send("SERVICE_ID", "TEMPLATE_ID", {
    to_email: toEmail,
    photo_attachment: base64,
  }, "PUBLIC_KEY");
}
```

> 대안: 발송량이 많아지거나 첨부 용량 제약이 문제가 되면 Formspree, Web3Forms 같은 유사 서비스로 교체하거나, 추후 서버(예: GitHub Actions + 이메일 API, 또는 별도 서버리스 함수)로 전환하는 방안도 열어둡니다.

---

## 6. 기술 스택

| 영역 | 선택 | 이유 |
|---|---|---|
| 마크업/스타일 | HTML5 + CSS3 | 빌드 도구 없이 GitHub Pages에 바로 배포 |
| 로직 | Vanilla JavaScript (ES Modules) | 프레임워크 빌드 과정 불필요, 배포 단순화 |
| 촬영/합성 | `getUserMedia` + `<canvas>` | 표준 웹 API만으로 촬영·보정·합성 처리 |
| 인쇄 | `window.print()` + Web Share API | 별도 서버 없이 OS 인쇄 파이프라인 활용 |
| 이메일 | EmailJS | 서버 없는 정적 사이트에서 첨부 이메일 발송 가능 |

---

## 7. 저장소 구조

```
life-four-cut/
├── index.html              # 전체 화면(프레임 선택~완성) SPA 형태로 구성
├── style.css
├── frames/
│   ├── frames.json         # 프레임 목록 및 슬롯 좌표 매니페스트
│   ├── frame_pink.png
│   ├── frame_mono.png
│   └── ...
├── js/
│   ├── main.js              # 화면 전환(라우팅) 총괄
│   ├── camera.js             # getUserMedia, 카운트다운, 6컷 캡처
│   ├── editor.js             # 보정 슬라이더/필터, 선택·순서 UI
│   ├── compositor.js         # 프레임 슬롯 좌표 기반 canvas 합성
│   ├── print.js               # window.print / Web Share 분기
│   └── email.js               # EmailJS 연동
└── README.md
```

---

## 8. 개발 로드맵 (Phase별)

| Phase | 범위 | 산출물 |
|---|---|---|
| 1 | 프레임 선택 화면 + `frames.json` 로딩 | 프레임 갤러리 UI |
| 2 | 카메라 권한/프리뷰 + 6컷 카운트다운 촬영 | 촬영 화면, 6장 캡처 로직 |
| 3 | 보정(슬라이더/필터) + 사진 선택·순서 UI | 편집 화면 |
| 4 | 슬롯 좌표 기반 프레임 합성(compositor.js) | 최종 미리보기 |
| 5 | 인쇄 연동(데스크톱/모바일 분기) + EmailJS 연동 | 인쇄·이메일 버튼 동작 |
| 6 | 실기기(노트북+프린터, 스마트폰) 통합 테스트 및 GitHub Pages 배포 | 배포 완료 |

---

## 9. 배포 방법 (GitHub Pages)

1. GitHub 저장소 생성 후 위 구조로 파일 푸시
2. 저장소 **Settings → Pages** 에서 배포 브랜치(예: `main`)와 루트 폴더(`/`) 지정
3. 발급된 `https://<계정명>.github.io/<저장소명>/` 주소로 접속 확인
4. `getUserMedia`(카메라)와 `navigator.share`(공유/인쇄)는 **HTTPS 환경에서만 동작** — GitHub Pages는 기본적으로 HTTPS이므로 별도 조치 불필요
5. EmailJS 콘솔에서 이 배포 주소를 허용 도메인으로 등록

---

## 10. 알려진 한계 및 리스크

- 브라우저는 보안 정책상 프린터를 직접 열거·자동 인쇄할 수 없어, 최종적으로는 **사용자가 인쇄 대화상자/공유 시트에서 한 번 확인**하는 단계가 반드시 남음 (완전한 "원클릭 무확인 인쇄"는 웹 표준상 불가능)
- EmailJS 무료 플랜은 발송량·첨부 용량에 제한이 있어, 트래픽이 늘어나면 유료 전환 또는 대안 서비스 검토 필요
- 모바일 브라우저(특히 iOS Safari)는 카메라 권한, `navigator.share` 동작이 버전에 따라 편차가 있어 실기기 QA가 중요
- 보정 기능은 슬라이더/필터 수준이며, AI 기반 얼굴 보정은 범위에 포함하지 않음(향후 확장 가능)

---

## 11. 향후 확장 아이디어

- 특정 감열식 미니 포토프린터(블루투스) 전용 연동으로 진짜 "즉석 인화" 구현
- QR코드로 완성 이미지를 다른 기기에서 다운로드
- 프레임에 스티커/텍스트 꾸미기 레이어 추가
- 배경 제거(그린스크린 없이) 등 AI 보정 옵션
